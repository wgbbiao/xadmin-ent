// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/wgbbiao/xadminent/ent/contenttype"
)

// ContentTypeCreate is the builder for creating a ContentType entity.
type ContentTypeCreate struct {
	config
	mutation *ContentTypeMutation
	hooks    []Hook
}

// SetAppLabel sets the "app_label" field.
func (ctc *ContentTypeCreate) SetAppLabel(s string) *ContentTypeCreate {
	ctc.mutation.SetAppLabel(s)
	return ctc
}

// SetModel sets the "model" field.
func (ctc *ContentTypeCreate) SetModel(s string) *ContentTypeCreate {
	ctc.mutation.SetModel(s)
	return ctc
}

// Mutation returns the ContentTypeMutation object of the builder.
func (ctc *ContentTypeCreate) Mutation() *ContentTypeMutation {
	return ctc.mutation
}

// Save creates the ContentType in the database.
func (ctc *ContentTypeCreate) Save(ctx context.Context) (*ContentType, error) {
	var (
		err  error
		node *ContentType
	)
	if len(ctc.hooks) == 0 {
		if err = ctc.check(); err != nil {
			return nil, err
		}
		node, err = ctc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*ContentTypeMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = ctc.check(); err != nil {
				return nil, err
			}
			ctc.mutation = mutation
			if node, err = ctc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(ctc.hooks) - 1; i >= 0; i-- {
			if ctc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = ctc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, ctc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (ctc *ContentTypeCreate) SaveX(ctx context.Context) *ContentType {
	v, err := ctc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (ctc *ContentTypeCreate) Exec(ctx context.Context) error {
	_, err := ctc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ctc *ContentTypeCreate) ExecX(ctx context.Context) {
	if err := ctc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (ctc *ContentTypeCreate) check() error {
	if _, ok := ctc.mutation.AppLabel(); !ok {
		return &ValidationError{Name: "app_label", err: errors.New(`ent: missing required field "ContentType.app_label"`)}
	}
	if v, ok := ctc.mutation.AppLabel(); ok {
		if err := contenttype.AppLabelValidator(v); err != nil {
			return &ValidationError{Name: "app_label", err: fmt.Errorf(`ent: validator failed for field "ContentType.app_label": %w`, err)}
		}
	}
	if _, ok := ctc.mutation.Model(); !ok {
		return &ValidationError{Name: "model", err: errors.New(`ent: missing required field "ContentType.model"`)}
	}
	if v, ok := ctc.mutation.Model(); ok {
		if err := contenttype.ModelValidator(v); err != nil {
			return &ValidationError{Name: "model", err: fmt.Errorf(`ent: validator failed for field "ContentType.model": %w`, err)}
		}
	}
	return nil
}

func (ctc *ContentTypeCreate) sqlSave(ctx context.Context) (*ContentType, error) {
	_node, _spec := ctc.createSpec()
	if err := sqlgraph.CreateNode(ctx, ctc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (ctc *ContentTypeCreate) createSpec() (*ContentType, *sqlgraph.CreateSpec) {
	var (
		_node = &ContentType{config: ctc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: contenttype.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: contenttype.FieldID,
			},
		}
	)
	if value, ok := ctc.mutation.AppLabel(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: contenttype.FieldAppLabel,
		})
		_node.AppLabel = value
	}
	if value, ok := ctc.mutation.Model(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: contenttype.FieldModel,
		})
		_node.Model = value
	}
	return _node, _spec
}

// ContentTypeCreateBulk is the builder for creating many ContentType entities in bulk.
type ContentTypeCreateBulk struct {
	config
	builders []*ContentTypeCreate
}

// Save creates the ContentType entities in the database.
func (ctcb *ContentTypeCreateBulk) Save(ctx context.Context) ([]*ContentType, error) {
	specs := make([]*sqlgraph.CreateSpec, len(ctcb.builders))
	nodes := make([]*ContentType, len(ctcb.builders))
	mutators := make([]Mutator, len(ctcb.builders))
	for i := range ctcb.builders {
		func(i int, root context.Context) {
			builder := ctcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*ContentTypeMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, ctcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, ctcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{err.Error(), err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, ctcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (ctcb *ContentTypeCreateBulk) SaveX(ctx context.Context) []*ContentType {
	v, err := ctcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (ctcb *ContentTypeCreateBulk) Exec(ctx context.Context) error {
	_, err := ctcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ctcb *ContentTypeCreateBulk) ExecX(ctx context.Context) {
	if err := ctcb.Exec(ctx); err != nil {
		panic(err)
	}
}
